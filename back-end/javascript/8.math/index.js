// Math = built-in object that provides a collection of porperties and methods

// console.log(Math.PI);
// console.log(Math.E);

let x = 3;
let y = 2;
let z = 1;

// z = Math.round(x); // 0.499 and below round down, and 0.500 and above round up
// z = Math.floor(x); // always round down
// z = Math.ceil(x); // always round up
// z = Math.trunc(x); // remove any decimal portion
// z = Math.pow(x, y); // first value on the power of the second value
// z = Math.sqrt(x); // square root of the value
// z = Math.log(x); // logarithm
// z = Math.sin(x); // sin (trigonometry)
// z = Math.cos(x); // cosin (trigonometry)
// z = Math.tan(x); // tangent (trigonometry)
// z = Math.abs(x); // absolute value (positive value of a negative number)
// z = Math.sign(x); // any negative value gonna be -1, and any positive value will be 1, zero will always be 0

// console.log(z);

// let max = Math.max(x, y, z); // max value
let min = Math.min(x, y, z); // min value

console.log(min);