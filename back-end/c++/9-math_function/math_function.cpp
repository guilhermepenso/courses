#include <iostream>
#include <cmath> // https://cplusplus.com/reference/cmath - all functions

int main() {

    double x = 3;
    double y = 4;
    double z;

    // z = std::max(x, y);
    // z = std::min(x, y);
    
    // z = pow(2, 4); // #CMATH - pow = primeiro número elevado ao segundo número

    // z = sqrt(9); // #CMATH - sqrt = raiz quadradra do número

    // z = abs(-3); // #CMATH - abs = número inteiro positivo

    // z = round(3.14); // #CMATH - round = número arredondado 
    // z = ceil(3.14); // #CMATH - ceil = número arredondado pra cima
    // z = floor(3.99); // #CMATH - ceil = número arredond pra baixo


    std::cout << z;

    return 0;
}